Template input file for newWRtool.py.

IMPORTANT NOTE: the value of the keys has to be written in the line that immediately follows the [key] label. If you don't want to set a key, comment it with a '#': #[key]. Commented lines are not considered.

##################################################################
##################### REQUIRED INPUT PATHS/NAMES (have to be set!) #################

# Name of the run
[exp_name]
test_ignazio_ensemble

# Directory of input files
[cart_in]
/data-archimede/seasonal/

# Output data directory. (for each run, a subfolder /exp_name/ is created inside cart_out_general/)
[cart_out_general]
/home/fabiano/Research/lavori/WeatherRegimes/

############## INPUT FILES. Set Ensemble flag. Specify either a filelist or enter the filenames directly. One amongst filelist and filenames has to be commented. #########################

# Flag to read ensembles. If set to True, each element in the filelist or filenames is treated as a partial filename: the code looks for all files that match that partial name. Partial naming accepts special character *.
[is_ensemble]
True

# Option to treat ensembles: 'all' puts together all runs as if it were a single run; 'member' separates the analysis for each ensemble, treating it as a group; 'year' puts all members together and separates the analysis for years, treating each year as a group. AAAAAA -- ONLY ALL IS ACTIVE FOR NOW!
[ens_option]
all

# Absolute path to the file that contains list of input files, one per line, with their relative path from cart_in/.
#[filelist]

# List of input files. If more than one file, list them in the same line, separated by commas.
[filenames]
ecmwfS5/daily/may/25_ens_members/zg500_ecmwf_Aday_1999_m05_144by73_in_m_member*.nc

# Name of the dataset. A name for each file is expected. If commented, automatic naming is applied: ens_1, ens_2, ...
[model_names]
ecmwfS5

# Select year range (two values separated by comma) # if commented it takes all yrs available
#[year_range]
1979, 2014

###################
#### Group options. For meaningful visualization
########

# Define a dictionary to group models in different categories. A comparison between the two groups will be done.
#[groups]
HR : CMCC-CM2-VHR4, EC-Earth-3-HR, ECMWF-IFS-HR, HadGEM3-GC31-HM, MPI-ESM1-2-XR
LR : CMCC-CM2-HR4, EC-Earth-3-LR, ECMWF-IFS-LR, HadGEM3-GC31-MM, MPI-ESM1-2-HR

# How should I compare groups? Possibilities: 'mean': visually groups the groups and computes the mean and stddev of the group, '1vs1' compares each element of a group with the corresponding elements of the other groups, 'both' does both comparisons.
#[group_compare_style]
both

# Specify group symbols (for scatter plots).
#[group_symbols]
HR: $H$
LR: $L$

# Specify reference group (differences are shown wrt this group):
#[reference_group]
LR


##########################################################################
##############  observations (reference)  ################

# Compare with ERA?
[run_compare]
True

# Use reference EOF space for all models?  ###### Vincola EOF to OBS (true)
[use_reference_eofs]
True

# Name to be assigned to the observations.
[obs_name]
ERA_is_obs

# Name of the file with the observed fields
[ERA_ref_orig]
/data-hobbes/fabiano/PRIMAVERA/highres_SST/zg500_ERAInterim_1979-2014.nc

# Name of the subfolder of cart_out_general for the observations. The program checks for existing EOF solver and clusters in cart_out_general/ERA_ref_folder.
[ERA_ref_folder]
ERA_ref_remap25

##########################################################################
##############  options for EOFs/CLUSTERING  ################

# Number of EOFs to be used in the decomposition:
# (if flag_perc is True, this value is ignored)
[numpcs]
4

# Consider a number of pcs to explain some fraction of the total variance (perc)? (if this is set to True, numpcs is ignored. otherwise perc is ignored)  ######## false uses numpcs , true uses percentage of variance
[flag_perc]
False

# Percentage of variance explained by the EOFs (0-100):
# (if flag_perc is False, this value is ignored)
[perc]
80

# Number of clusters to be used:
[numclus]
5

# Calculate significance of clustering (run cluster_sig)  ######## if true computing time *10
[run_sig_calc]
False

# Save heavy output? (global anomalies, transitions, solver, ...)
[heavy_output]
False

##########################################################################
##############  options for PRECOMPUTE -- preprocessing  ################

# Atmospheric level at which the variable is extracted (if more levels are present)  #### livello atmosferico *ma gia' obbligato in input files che hanno solo livello, else sceglie lui
[level]
500

# Season to be selected (options: JJA, DJF, DJFM, NDJFM)
[season]
JJA

# Regional average ('EAT': Euro-Atlantic, 'PNA': Pacific North American, 'NH': Northern Hemisphere, 'Eu' for Europe, 'Med' for Mediterranean)
# Area to be selected (options: EAT, PNA, NH)
[area]
custom

# Custom area (activated only if [area] is set to 'custom')
[custom_area]
-10, 80, 25, 60

##########################################################################
###################  other options  ###################

# Pattern NAMES
#[patnames]
NAO +, Sc. Blocking, Atl. Ridge, NAO -

# Pattern tags
#[patnames_short]
NP, BL, AR, NN

# Visualization: standard, Nstereo, Sstereo
[visualization]
standard

# Equatorward bounding latitude for polar/stereo plots
[bounding_lat]
30

# Margins of the plot for visualization 'standard'. If no margins are specified, the whole globe is showed.
[plot_margins]
-10, 80, 25, 60
